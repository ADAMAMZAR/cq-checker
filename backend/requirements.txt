# ── Core ──────────────────────────────────────────────────────────
fastapi==0.99.1
uvicorn==0.30.1
pydantic>=1.10.0,<2.0.0
python-multipart==0.0.9
python-dotenv==1.0.1
requests==2.32.3

# ── Database (Neon / pgvector) ───────────────────────────────────
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
alembic==1.13.2
pgvector==0.3.0

# ── Storage (GCS — activated in Phase 8) ────────────────────────
google-cloud-storage==2.18.0

# ── AI Providers ─────────────────────────────────────────────────
google-generativeai==0.5.4
google-cloud-aiplatform==1.59.0
pymupdf==1.28.0

# ── Document parsing (Docling — IBM OSS) ────────────────────────
# Single-call OCR + layout + table extraction for PDF, image, DOCX, XLSX, PPTX, HTML.
# Pinned to the 2.x major; bump 2.55.1 -> 2.118.0 if your env supports it.
docling>=2.55.0,<3.0.0
# Optional Docling backends (kept on separate lines so they can be removed
# one at a time if a deploy target doesn't need them).
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=1.0.0
Pillow>=10.3.0

# ── Testing ──────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==0.24.0
httpx==0.27.0
